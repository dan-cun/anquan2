# Copy to benchmark/runtime.env. This ignored file is the shared local secret
# source for online and benchmark backends. Never commit the populated file.
SECMIND_LLM_API_KEY=replace-with-benchmark-api-key
