<instructions>
任务：基于 AI Agent 执行日志，生成一份简洁、按时间顺序的摘要，展示进度、成功与挑战。

数据：
- <execution_logs> 包含 AI Agent 在子任务执行期间所采取行动的带时间戳日志。
- 每条 <log> 条目代表 Agent 执行的一个具体行动或步骤。
- 日志条目包括：subtask_id、type、message（尝试执行的操作）和 result（结果）。
- 多条日志可能属于同一子任务，展示逐步推进直至完成的过程。

要求：
1. 构建连贯的叙述，按时间顺序展示工作进展。
2. 突出关键里程碑、成功和重要发现。
3. 识别遇到的挑战及其处理方式（或未处理的情况）。
4. 保留关于所采取行动及其结果的关键技术细节。
5. 指明哪些任务成功完成，哪些遇到了问题。
6. 捕捉 AI Agent 用于解决问题的推理过程和方法。
7. 排除无助于理解进展的常规或重复性操作。

格式：
- 以流畅的叙述形式呈现，描述发生了什么，而非事件列表。
- 使用客观语言，聚焦于行动和结果。
- 将贡献于同一子任务或目标的关联行动进行分组。
- 强调转折点、突破和重大障碍。
- 不要在摘要中保留 XML 标记。
- 在细节与简洁之间取得平衡，以保持可读性。

摘要应使读者清晰了解工作如何推进、完成了什么、面临哪些挑战，以及这些挑战被克服的有效程度。
</instructions>

<execution_logs>
  {{range .MsgLogs}}
  <log>
	<subtask_id>{{.SubtaskID}}</subtask_id>
	<type>{{.Type}}</type>
	<message>{{.Message}}</message>
	<result>{{.Result}}</result>
  </log>
  {{end}}
</execution_logs>