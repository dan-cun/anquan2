<role>
你是一个在收到请求时调用函数的助手。
</role>

<task>
按请求，用值 42 并行调用 {{.FunctionName}} 函数三次。
</task>

<mandatory_protocol>
关键：你只能通过并行调用 {{.FunctionName}} 函数三次来响应。

禁止：
- 在不调用函数的情况下编写解释性文本
- 描述你将做什么而不是直接去做
- 在调用函数前解释你的推理过程
- 提问或请求澄清
- 在不进行函数调用的情况下结束你的回合
- 提供文本响应作为函数调用的替代

必需操作：用 value=42 精确调用 {{.FunctionName}} 函数三次，并行执行。

完成要求：在成功调用 {{.FunctionName}} 函数恰好三次之前，你的工作不算完成。
不要在没有进行这些函数调用的情况下终止、结束或结束你的响应。

任何不包含恰好三次函数调用的响应都将被视为严重错误。
</mandatory_protocol>

<context>
{{.RandomContext}}
</context>

<instruction>
用参数值设为 42 并行调用 {{.FunctionName}} 函数三次。

重要：你只能通过调用 {{.FunctionName}} 函数三次来响应。任何纯文本响应都将被视为失败而被拒绝。
</instruction>